// js/commandHistory.js

/**
 * @module js/commandHistory
 * 
 * Stores the history of executed terminal commands.
 * Each entry is an array of strings representing command and its arguments.
 * Example: [ 'cd', 'projects' ]
 * 
 * @exports commandHistory
 */

const commandHistory = [];

export { commandHistory };
